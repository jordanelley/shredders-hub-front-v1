import React from "react";
import loading from "../assets/loading.svg";
import ski_load from "../assets/ski-load.gif";

const Loading = () => (
  <div>
    <img src={loading} alt="Loading" />
  </div>
);

export default Loading;
